# نقطه ادامه پروژه مدیریت فروشگاه

این فایل پس از هر تحویل کوچک به‌روزرسانی می‌شود تا ادامه کار با Resume به تاریخچه گفتگو وابسته نباشد.

## آخرین تحویل پایدار

- شاخه کاری: `codex/admin-redesign`
- آخرین commit پایدار: `fb518ad` — فاز ۲۸ مرکز یکپارچه یادآوری برای فروش نسیه، دفتر و چک
- فاز آماده commit: فاز ۲۹ اتصال canonical فروش نسیه به مانده مشتری و تخصیص تسویه
- تست‌ها: کل backend برابر ۲۲۶ موفق؛ مجموعه هدفمند فاز ۲۹ برابر ۷۰؛ نقش‌ها ۱۷؛ اعداد ۴؛ frontend build با ۹۷۸ ماژول موفق
- داده: هیچ migration یا backfill روی production اجرا نشده است. مسیر runtime هاست به دیتابیس اصلی اصلاح و بکاپ تأییدشده آن ساخته شد.

## کار بعدی

1. QA دستی فازهای ۲۷ تا ۲۹ در Chrome دسکتاپ و موبایل.
2. طراحی command مستقل reconciliation برای فروش‌های قدیمی با dry-run و گروه‌های missing/exact/conflict؛ بدون اجرای production تا تأیید.
3. اجرای کنترل‌شده نخستین bundle/restore آزمایشی production فقط پس از تأیید کاربر.
4. فاز BAK-002: انتخاب مقصد رمزنگاری‌شده off-host و روش اعلان شکست، سپس فعال‌سازی Scheduled Task.
5. QA دستی فاز ۲۴ برای caret، paste و Backspace در Chrome دسکتاپ و موبایل.
6. QA تعاملی پنج نقش در Chrome دسکتاپ و موبایل.

## شکاف‌های اثبات‌شده در ممیزی

- popup مانده مشتری برای فروش‌های جدید بدهی نسیه را از دفتر canonical منعکس می‌کند؛ reconciliation داده‌های قدیمی عمداً باقی است.
- پرداخت pending فروش در backend، UI و مرکز یادآوری یکپارچه شده است؛ QA تعاملی باقی مانده است.
- مرکز یادآوری فیلتر یکپارچه شخص، نوع و افق زمانی و محدودسازی بر اساس سطح دسترسی دارد.
- QA مرورگری کامل همه نقش‌ها و تعامل‌های موبایل هنوز انجام نشده و نباید انجام‌شده تلقی شود.

## رخداد ایمنی داده محلی

- هنگام تست فاز ۲۶، فایل توسعه `backend/store.db` ناخواسته تا migration `0018_payment_due_audits` ارتقا یافت. migration افزایشی بود، بررسی یکپارچگی `ok` است و هر دو رکورد پرداخت قبلی حفظ شده‌اند.
- برای جلوگیری از حذف جدول audit، downgrade انجام نشد.
- فوراً یک بکاپ تأییدشده خارج از مخزن در `C:\Users\Mesut\StoreBackups\store-20260909T062428.862928Z.db` ساخته شد. دیتابیس production هیچ تغییری نکرده است.

## رخداد مسیر دیتابیس هاست

- در ۱۴۰۵/۰۶/۱۸ مشخص شد runtime هاست به فایل خالی `/home/Hobubat/Store/backend/store.db` متصل است، در حالی که داده‌های واقعی در `/home/Hobubat/store-data/store.db` سالم بودند.
- پیش از اصلاح، فایل تنظیمات در `/home/Hobubat/.store.env.pre-db-fix-20260909T155227Z` نگهداری شد؛ سپس فقط `DATABASE_URL` به مسیر اصلی برگشت و Reload موفق بود.
- بکاپ آنلاین و integrity-checked دیتابیس واقعی در `/home/Hobubat/backups/store-live-pre-login-fix-20260909T155422Z.db` ساخته شد؛ شامل ۱ کاربر و ۱۸ شخص. هیچ رمز یا رکورد مالی تغییر نکرد.

پس از موارد بالا، backlog از نخستین مورد ناقص با یک سند و commit مستقل برای هر برش ادامه می‌یابد.

## محدودیت انتشار

هیچ merge به `main`، push یا deploy تا بازبینی و تأیید صریح کاربر انجام نشود.
