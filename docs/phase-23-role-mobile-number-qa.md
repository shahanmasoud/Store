# فاز ۲۳ — تثبیت نقش‌ها و ورودی‌های فارسی

## تحویل

- policy دسترسی صفحه‌ها از رابط جدا و با هر ۱۶ ترکیب چهار مجوز تست شد.
- دکمه ابطال چک برای زیرمدیر پنهان شد؛ backend نیز ابطال را برای مدیر اصلی نگه می‌دارد.
- dialog رویداد چک در موبایل تمام‌صفحه شد.
- مقدار خرید و فروش با ورودی اعشاری فارسی، grouping و حفظ caret یکپارچه شد؛ state و API همچنان canonical انگلیسی می‌مانند.
- ساعت خرید، تلفن شخص، تاریخ قیمت و تاریخ شروع تخفیف فارسی و مناسب صفحه‌کلید موبایل شدند.

## تست خودکار

- تست policy نقش‌ها: `17 passed`.
- تست utility اعداد: `4 passed`.
- build تولید: موفق با `978 modules transformed`.
- تست backend فاز قبل: `201 passed` و در این برش backend تغییر نکرد.

## QA واقعی Chrome

- محیط جداگانه `.qa-role.db` از migration صفر تا 0017 ساخته و seed شد؛ دیتابیس اصلی تغییر نکرد.
- صفحه ورود دسکتاپ و نمای موبایل `390 × 844` واقعاً render شد.
- در موبایل اندازه viewport دقیق بود و `scrollWidth = 390`؛ هم‌پوشانی یا overflow افقی در صفحه ورود دیده نشد.
- داشبورد مدیر در Chrome با داده QA render و اعداد فارسی تأیید شد.
- افزونه کنترل Chrome پس از reload رویدادهای click/type را به React نرساند؛ بنابراین ماتریس تعاملی پنج نقش «تکمیل‌شده» اعلام نمی‌شود. policy همان نقش‌ها فعلاً با تست مستقل ۱۷ حالته پوشش داده شده است.

## تست دستی بعدی

در Chrome با نقش‌های فروش، انبار، دفتر، چک/گزارش و ترکیبی ورود کنید؛ منو و کارت‌ها باید union دقیق مجوزها باشند. در `390 × 844` Drawer، فرم‌ها، dialogها، CTAهای ۴۴ پیکسلی، صفحه‌کلید عددی و نبود overflow را بررسی کنید.

## باقی‌مانده

quantityهای کم‌تکرار انبار، عمده‌فروشی، تخفیف و آنلاین باید در برش بعد به ورودی مشترک منتقل شوند. QA تعاملی پنج نقش و تست بازیابی بکاپ production نیز باقی است.
